# Archivo de ejemplo para variables de entorno en Docker
# Este archivo ya está configurado y listo para usar con docker-compose
# No necesitas modificar nada, docker-compose.yml ya tiene estas variables

# Database (configurado automáticamente por docker-compose)
DATABASE_URL=postgresql://social_user:social_pass_2024@postgres:5432/social_media_db

# NextAuth
NEXTAUTH_SECRET=dev-secret-key-change-in-production-12345678
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (opcional - déjalo vacío si no lo necesitas)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=

# Supabase (compatibilidad - no se usa en modo Docker)
NEXT_PUBLIC_SUPABASE_URL=http://localhost:3000
NEXT_PUBLIC_SUPABASE_ANON_KEY=dummy-key-for-compatibility
SUPABASE_SERVICE_ROLE_KEY=dummy-key-for-compatibility
